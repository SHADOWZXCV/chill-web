const { chai, app } = require('Test');
const should = chai.should();

describe('User signing out...', () => {
    it("User is valid, already signed in and trying to sign out", done => {
        
    });
 })